# Not part of the packaged app. Stanzas ship from src/package/default/inputs.conf (see `make package`).
# DAX `gdax_download*` file monitor was removed; index OHLCV is ingested only via `update_stocks.py` (and optional one-shot `stocks_history.csv` monitor).
