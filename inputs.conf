# Not part of the packaged app. Stanzas ship from src/package/default/inputs.conf (see `make package`).
# DAX file monitor was removed. Stock index data uses `default/inputs.conf` + `update_stock_indices.py` → `s4c_stock_indices` (no shipped CSV baseline; first run fetches history).
