import SplunkSearch from '../../components/SplunkSearch.js'
import Link from '@splunk/react-ui/Link'


<Link to="https://bit.ly/s4c_quiz" openInNewContext>"Splunk4Champions Quiz Online"</Link>

## 1. What is it?
<img src="/static/app/splunk4champions2/images/rebus.png" width="600" />
___

## 2. Which search is better? And why?
<SplunkSearch earliest="-1y" latest="now" spl={`index="s4c_meteo" Frankfurt
| rename weather_code as WeatherCode 
| stats  count as eventcount by WeatherCode 
| where eventcount > 5000 `}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-1y" latest="now" spl={`index="s4c_meteo" Frankfurt
| stats  count as eventcount by weather_code 
| where eventcount > 5000 
| rename weather_code as WeatherCode`}>
</SplunkSearch>

___

## 3. Which search is better? And why?
<SplunkSearch earliest="-30d" latest="now" spl={`index=_internal 127.0.0.1 `}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-30d" latest="now" spl={`index=_internal TERM(127.0.0.1)`}>
</SplunkSearch>

___

## 4. Which search is better? And why?
<SplunkSearch earliest="-30d" latest="now" spl={`index="_introspection" component=PerProcess "data.process"=splunkd
 | stats last(_time) as _time, count as eventcount by "data.pid"
 | rename "data.pid" as processId 
 | where eventcount > 10`}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-30d" latest="now" spl={`index="_introspection"  "data.process"=splunkd 
 | transaction "data.pid"
 | rename "data.pid" as processId 
 | table _time, processId, eventcount
 | search eventcount > 10`}>
</SplunkSearch>

___

## 5. Which search is better? And why?
<SplunkSearch earliest="-30d" latest="now" spl={`index="_audit" 
| join type=left host [search index="_audit" 
| stats count as eventcount by host]`}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-30d" latest="now" spl={`index="_audit" 
| eventstats count as host_count by host 
| stats count by component, host_count, host`}>
</SplunkSearch>

___

## 6. Which search is better? And why?
<SplunkSearch earliest="-1y" latest="now" spl={`index=s4c_meteo 
| stats count by city `}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-1y" latest="now" spl={`|tstats count  WHERE index="s4c_meteo" by city`}>
</SplunkSearch>

___

## 7. Which search is better? And why?
<SplunkSearch earliest="-1y" latest="now" spl={`|tstats avg(temperature_2m) max(temperature_2m) min(temperature_2m)  median(temperature_2m) 
 WHERE index=s4c_meteo by city`}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-1y" latest="now" spl={`index=s4c_meteo 
|stats avg(temperature_2m) max(temperature_2m) min(temperature_2m)  median(temperature_2m) by city`}>
</SplunkSearch>

___

## 8. Which search is better? And why?
<SplunkSearch earliest="-2y" latest="now" spl={`index="s4c_meteo"  city IN(Berlin Brussels Warsaw)`}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-2y" latest="now" spl={`index="s4c_meteo"  
| where city IN("Berlin", "Brussels", "Warsaw")`}>
</SplunkSearch>

___

## 9. Which search is better? And why?
<SplunkSearch earliest="-2y" latest="now" spl={`index="s4c_meteo" *erlin`}>
</SplunkSearch>

versus: 

<SplunkSearch earliest="-2y" latest="now" spl={`index="s4c_meteo" Ber*`}>
</SplunkSearch>