import IndexHealth from '../../components/IndexHealth.js'
import DisplayModeToggle from '../../components/DisplayModeToggle.js'
import LabSchemeToggle from '../../components/LabSchemeToggle.js'

<DisplayModeToggle />

<IndexHealth />

> **If any index shows "No data":** go to **Settings → Data → Indexes** and create the missing index, then enable its data source under **Settings → Data inputs → Files & Directories**.

<LabSchemeToggle />
