# Workshop joins: meteo historic (~26k rows) and long-range stock data exceed the
# default subsearch / join output cap (~10k), which drops matches and shows empty
# weather / stock fields. Safe increase for the lab app on the search head.
# https://docs.splunk.com/Documentation/Splunk/latest/admin/Limitsconf

[subsearch]
maxout = 100000
